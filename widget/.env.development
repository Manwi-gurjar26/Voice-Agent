# Used by `npm run dev` and local dev builds only. Production builds must
# set VITE_API_BASE_URL explicitly at build time — see README; this file is
# not the place to put a guessed production URL.
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
